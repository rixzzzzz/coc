name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build COC AutoFarmer (x86_64)
    runs-on: ubuntu-24.04        # LTS pinne = environnement stable (pas de PPA casse)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip openjdk-17-jdk autoconf libtool pkg-config \
            zlib1g-dev libncurses-dev cmake libffi-dev libssl-dev \
            build-essential ccache libltdl-dev

      - name: Install Buildozer + Cython
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install --upgrade buildozer "cython==0.29.36"

      # === LE FIX : force x86_64 pour MuMu / LDPlayer ===
      - name: Force x86_64 architecture
        run: |
          sed -i 's/^android\.archs *=.*/android.archs = x86_64/' buildozer.spec
          grep -nE '^android\.archs' buildozer.spec

      - name: Cache Buildozer SDK/NDK
        uses: actions/cache@v4
        with:
          path: ~/.buildozer
          key: buildozer-${{ hashFiles('buildozer.spec') }}

      - name: Build APK (debug)
        env:
          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
        run: |
          yes | buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: cocfarmer-x86_64-debug
          path: bin/*.apk
